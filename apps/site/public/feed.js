// Intentionally empty.
//
// The feed used to ship a JS toggle handler for same-document View
// Transitions and one-open-at-a-time behavior on <details>. We dropped
// it in favor of native <details> + a CSS animation on ::details-content.
// No JS needed for the open/close behavior.
//
// This file is kept (rather than deleted) so old cached pages that still
// reference /feed.js fail silently rather than 404.
