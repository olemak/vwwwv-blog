-- schema.sql — canonical reference for the current schema.
-- This file is a snapshot of where migrations have brought us. The actual
-- schema is applied via files in migrations/, in order. Use this for reading;
-- it should match what `migrations/` produces when run from scratch.

-- See migrations/0001_initial.sql for the authoritative initial schema.
-- Future migrations will live alongside it (0002_*.sql, 0003_*.sql, …).
-- When a migration changes the schema, update this file by hand to keep
-- it in sync; treat any drift as a bug.

-- For now, schema = migrations/0001_initial.sql verbatim.
